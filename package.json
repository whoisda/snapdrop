{
  "private": true,
  "scripts": {
    "postinstall": "lerna run postinstall --scope @onedoes/snapdrop-pwa",
    "build": "lerna run build --stream",
    "start": "lerna run start --stream --parallel",
    "test": "lerna run test --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^2.1.2"
  }
}
